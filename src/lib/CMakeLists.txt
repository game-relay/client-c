# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(game-relay-client STATIC
  client.c
  connector.c
  debug.c
  listener.c
  socket.c)

include(Tornado.cmake)
set_tornado(game-relay-client)

target_include_directories(game-relay-client PUBLIC ../include)


target_link_libraries(game-relay-client PUBLIC
  game-relay-serialize
  datagram-transport
  monotonic-time
  discoid)

